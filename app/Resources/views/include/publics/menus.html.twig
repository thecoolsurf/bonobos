<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar">&nbsp;</span>
            </button>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li id="menu_home">
                    <a href="{{ url('home') }}">Home</a></li>
                <li id="menu_about">
                    <a href="{{ path('about') }}">About</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li id="menu_members">
                    <a href="{{ path('members') }}"><span class="fa fa-users"></span> Members</a></li>
            {% if not is_granted('ROLE_USER') %}
                <li id="menu_register">
                    <a href="{{ path('fos_user_registration_register') }}"><span class="fa fa-user"></span> Sign Up</a></li>
                <li id="menu_resetting">
                    <a href="{{ path('fos_user_resetting_request') }}"><span class="fa fa-life-ring"></span> Password</a></li>
                <li id="menu_login">
                    <a href="{{ path('fos_user_security_login') }}"><span class="fa fa-sign-in"></span> Login</a></li>
            {% endif %}
            {% if is_granted('ROLE_USER') %}
                <li id="menu_friends">
                    <a href="{{ path('friends') }}"><span class="fa fa-users"></span> Friends</a></li>
                <li id="menu_profile">
                    <a href="{{ path('fos_user_profile_show') }}"><span class="fa fa-user"></span> My account</a></li>
                <li id="menu_edit">
                    <a href="{{ path('fos_user_profile_edit') }}"><span class="fa fa-pencil"></span> Modify</a></li>
                <li id="menu_logout">
                    <a href="{{ path('fos_user_security_logout') }}"><span class="fa fa-sign-out"></span> Logout</a></li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>
<div id="haut"><i class="fa fa-arrow-up"></i></div>
